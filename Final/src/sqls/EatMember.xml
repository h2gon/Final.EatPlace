<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="EatMember">

<insert id="addmember" parameterType="kh.c.five.model.MemberDto">
	INSERT INTO EATMEMBER(ID, PWD, NAME, EMAIL, AUTH)
	VALUES(#{id}, #{pwd}, #{name}, #{email}, 3)
</insert>

<select id="login" parameterType="kh.c.five.model.MemberDto"
	resultType="kh.c.five.model.MemberDto">
	SELECT ID, NAME, EMAIL, AUTH
	FROM EATMEMBER
	WHERE ID=#{id} AND PWD=#{pwd}
</select>

<select id="getID" 
	parameterType="kh.c.five.model.MemberDto"
	resultType="java.lang.Integer">
	SELECT NVL(COUNT(*), 0)
	FROM EATMEMBER
	WHERE ID=#{id}
</select>



</mapper>  









